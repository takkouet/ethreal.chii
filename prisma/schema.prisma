generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id          String   @id @default(cuid())
  name        String
  description String
  priceVnd    Int
  imageUrl    String
  stock       Int      @default(0)
  active      Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([active])
}

model Order {
  id            String        @id @default(cuid())
  orderNumber   String        @unique @default(dbgenerated("('CHII-' || upper(substr(md5(gen_random_uuid()::text), 1, 6)))"))
  customerName  String
  phone         String
  email         String?
  address       String
  note          String?
  paymentMethod PaymentMethod
  status        OrderStatus   @default(PENDING)
  totalVnd      Int
  accessToken   String        @default(dbgenerated("gen_random_uuid()::text"))
  items         OrderItem[]
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt

  @@index([createdAt])
}

model OrderItem {
  id          String  @id @default(cuid())
  orderId     String
  order       Order   @relation(fields: [orderId], references: [id], onDelete: Cascade)
  productId   String
  productName String
  priceVnd    Int
  quantity    Int
}

model IdempotencyKey {
  key         String   @id
  orderId     String
  accessToken String
  createdAt   DateTime @default(now())
}

model AdminUser {
  id           String @id @default(cuid())
  email        String @unique
  passwordHash String
}

enum PaymentMethod {
  COD
  BANK_QR
  CARD_CONTACT
}

enum OrderStatus {
  PENDING
  PAID
  SHIPPED
  CANCELLED
}
